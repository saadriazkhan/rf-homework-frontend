import { CSSProperties } from 'react';

export const CursorPointer: CSSProperties = {
    cursor: 'pointer'
};

export const BackdropBlackTransparent: CSSProperties = {
    background : '#0000008a'
};

export const DialogWidth: CSSProperties = {
    width: 800
};

export const DialogBody: CSSProperties = {
    maxHeight: 500,
    overflowY: 'auto'
};
